name: apm-plackage-cloudflare
version: 0.1.0
description: Agent configuration for Cloudflare Workers projects. Depends on the frontend package because Workers projects ship TypeScript and consume Astro / Vite tooling.

dependencies:
  apm:
    - mazrean/apm-plackage/common
    - mazrean/apm-plackage/frontend
