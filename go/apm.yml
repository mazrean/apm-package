name: apm-plackage-go
version: 0.1.0
description: Agent configuration for Go projects (libraries, CLIs, servers).

dependencies:
  apm:
    - mazrean/apm-plackage/common
    - mazrean/agent-skills/skills/using-go-tool-directive
